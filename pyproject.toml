[project]
name = "seal"
version = "0.1.0"
description = "A simple text tokenizer package"
authors = [{name = "Cureerel"}]
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
seal = "seal:main"